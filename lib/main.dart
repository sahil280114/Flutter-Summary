import 'package:flutter/material.dart';
import 'package:summary/iPhone6781.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  Widget build(BuildContext context){
    return Scaffold(
      body: new iPhone6781(),
    );
  }
}